import React from 'react';

export default class Note extends React.Component {
  render() {
    return (
    <div>
      <h1>Note Creator App</h1>
        <p>Dynamically add notes to a page</p>
    </div>
    );
  }
}
